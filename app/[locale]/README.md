Dynamic locale segment resolved by next-intl middleware to /en/ or /es/. layout.tsx loads fonts, sets html lang, injects hreflang tags, wraps in Header + Footer. page.tsx is the home page — imports section components in order: Hero → Problem → Services → Process → WhyUs → SocialProof → CtaBanner. All pages receive locale param and pass to useTranslations(). Never hardcode strings in any file in this folder.
