Single source of truth for all user-facing copy in both languages. routing.ts defines locales ['en','es'] and defaultLocale 'en'. en.json and es.json use identical flat dot-notation keys: section.element. Rules: every key in en.json must exist in es.json. Never add strings to components — add here first. Placeholder copy prefixed with [TODO]. Always update BOTH files in the same commit.
