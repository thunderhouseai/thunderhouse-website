React components organized into three layers. ui/: dumb primitive components (Button, Card, Badge, Input, BoltIcon, CountUp). sections/: full-width page sections, each self-contained with i18n and animation. layout/: persistent components rendered on every page (Header, Footer, LanguageSwitcher, ChatWidget). Never fetch data inside section components — all content from i18n JSON only.
